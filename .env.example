# Copy this file to .env and adjust values as needed.
JWT_SECRET=supersecretkey # auto-generated and persisted in /data if not set
PG_HOST=localhost # leave empty to use embedded Postgres
PG_USER=anchor
PG_PASSWORD=password
PG_DATABASE=anchor
PG_PORT=5432

# Optional: Homarr integration settings (server-to-server access)
# If not set, users can use their per-account API token from Settings.
# HOMARR_INTEGRATION_TOKEN=your-long-random-token
# HOMARR_INTEGRATION_USER_ID=anchor-user-uuid

# Homarr integration (optional)
# HOMARR_INTEGRATION_TOKEN=your-long-random-token
# HOMARR_INTEGRATION_USER_ID=uuid-of-anchor-user

# Optional: Set to 'disabled', 'enabled', or 'review' to lock sign up mode.
# If not set, admins can control it via the admin panel.
# USER_SIGNUP=enabled